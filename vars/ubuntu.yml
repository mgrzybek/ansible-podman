---

podman_repo_apt_url: "https://download.opensuse.org/repositories/devel:/kubic:/libcontainers:/stable/xUbuntu_${VERSION_ID}/"
podman_repo_key: "https://download.opensuse.org/repositories/devel:/kubic:/libcontainers:/stable/xUbuntu_${VERSION_ID}/Release.key"

podman_pre_distro_packages:
  - software-properties-common
  - uidmap

podman_distro_packages:
  - podman

podman_sysconfig: /etc/default

podman_containers_storage_zpool: containers
