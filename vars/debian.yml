---

podman_repo_apt_url: "ppa:projectatomic/ppa"

podman_pre_distro_packages:
  - software-properties-common
  - uidmap

podman_distro_packages:
  - podman

podman_sysconfig: /etc/default

podman_containers_storage_zpool: containers
